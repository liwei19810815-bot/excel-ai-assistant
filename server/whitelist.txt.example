# AI 助手白名单：一行一个 Windows 登录名（%USERNAME%），大小写不敏感。
# 以 # 开头的是注释。改完【不需要重启服务】，下一次请求即生效。
#
# 在名单里  → 任务窗格直接用 managed.json 里配的模型，用户什么都不用填
# 不在名单里 → 任务窗格显示设置页，用户自己填内网模型地址
#
# ⚠ 这里比对的用户名来自客户端（安装时写进 manifest 的 ?u=），
#   用户可以改。这是分流不是鉴权，别把它当访问控制用。

zhangsan
lisi
